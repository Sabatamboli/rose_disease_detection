services:
  - type: web
    name: rose-disease-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run main.py --server.port 10000 --server.address 0.0.0.0